table:
  schema: public
  name: submissions
insert_permissions:
  - role: public
    permission:
      check: {}
      columns:
        - commitment
        - community_id
        - proof_of_interaction
select_permissions:
  - role: public
    permission:
      columns:
        - approved
        - id
        - commitment
        - proof_of_interaction
        - created_at
        - updated_at
        - community_id
      filter: {}
